SET(KIT CFSQP)
SET(UKIT CFSQP)

SET(SOURCE_FILES cfsqp.c qld.c suCFSQPDLL.cpp)
FILE(GLOB INCLUDE_FILES *.h)

INCLUDE_DIRECTORIES(${NMBLApp_SOURCE_DIR} ${NMBLApp_SOURCE_DIR}/Vendors )

SET(EXPORT_MACRO "SU${UKIT}_EXPORTS,__STDC__")

ADD_LIBRARY(su${KIT} SHARED SOURCE_FILES INCLUDE_FILES) 

SET_TARGET_PROPERTIES(su${KIT} PROPERTIES
   DEFINE_SYMBOL ${EXPORT_MACRO}
)

INSTALL_TARGETS(${NMBLApp_BINARY_DIR}/LibDev/ su${KIT})